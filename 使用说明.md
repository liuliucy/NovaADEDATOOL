# 大壮的AD工具使用说明

适用版本：`1.0.6`  
适用系统：Windows x64

大壮的AD工具用于监听剪贴板中的立创商城（LCSC）元件编号，整理当前会话的元件清单，并导出 BOM CSV、KiCad 库或 Altium 库。

> 重要提醒
>
> - 历史记录和匹配结果只保存在当前运行会话中，退出软件后不会恢复。关闭软件前请先导出需要的数据。
> - 软件处于“监听中”时会读取新复制的文本。复制密码、密钥等敏感内容前，请先暂停监听。
> - BOM 元数据、KiCad/Altium 库生成和版本检查需要联网。
> - 当前发布包未做 Windows Authenticode 签名。若 SmartScreen 提示“未知发布者”，请先确认文件来源并核对本文末尾的 SHA-256，再决定是否运行。

## 1. 选择安装方式

| 版本 | 文件 | 使用方式 |
| --- | --- | --- |
| 安装版 | `大壮的AD工具_1.0.6_x64-setup.exe` | 双击并按安装向导完成安装 |
| 便携版 | `大壮的AD工具_1.0.6_x64_portable.zip` | 完整解压后运行文件夹内的 `大壮的AD工具.exe` |

便携版不要直接在 ZIP 压缩包内运行。建议解压到具有写入权限的目录，例如：

```text
D:\Tools\大壮的AD工具\
```

软件会把导出配置保存为程序同目录下的 `export_config.json`，因此不要把便携版放在只读目录。

## 2. 两分钟快速上手

1. 启动软件，确认左下角状态为“监听中”。
2. 打开立创商城元件页面，复制元件编号或包含元件编号的文本。
3. 软件可以识别 `C` 加至少 3 位数字的编号，例如 `C2040`、`C2927505`，字母大小写均可。
4. 返回“监听”页，在“匹配结果”中确认元件编号。
5. 需要清单时，在监听页设置 BOM CSV 文件路径并点击“导出 BOM CSV”。
6. 需要元件库时，进入“导出”页，设置输出目录后选择 KiCad 或 Altium 导出。

软件启动时不会把启动前已经存在于剪贴板中的旧内容直接加入记录。没有匹配时，请在软件启动后重新复制一次元件信息。

![监听页](manual-assets/监听页.png)

## 3. 监听页

### 3.1 识别规则

当前版本会从新复制的任意文本中提取全部 `C` 加至少 3 位数字的内容，例如：

```text
C2040
编号：C7470135
https://www.szlcsc.com/product-detail/C2927505.html
C2040, C25804, C2927505
```

界面保留“快速 ID”和“完整信息”两个匹配入口，日常使用建议保持两项均开启。当前 `1.0.6` 的实际提取规则以本文上方的 `C` 编号格式为准。

连续两次复制完全相同的文本时，第二次可能被视为重复剪贴板内容而忽略。匹配结果中的次数仅用于辅助统计，不应直接作为原理图或生产 BOM 的最终数量。

### 3.2 页面区域

| 区域或按钮 | 作用 |
| --- | --- |
| 监听中 / 已暂停 | 开始或暂停剪贴板采集 |
| 匹配结果 | 按 LCSC ID 去重显示已识别元件 |
| 复制 ID | 把全部唯一 ID 按“一行一个”复制到剪贴板 |
| 显示/隐藏按钮 | 展开或收起匹配列表，不会删除数据 |
| 剪贴板 | 显示最近一次捕获的完整文本 |
| 清空全部 | 二次确认后清空历史、匹配结果和本次导出状态 |

底部状态栏中：

- `H` 表示当前剪贴板历史条数。
- `M` 表示当前匹配到的唯一元件编号数。

匹配列表中的单条记录可以复制或删除。删除匹配结果不会自动删除对应历史，删除历史也不会自动删除已匹配元件。

## 4. 历史页

“历史”页按从新到旧显示当前会话捕获的非空剪贴板文本，包括没有匹配到元件编号的文本。

- “复制”会复制该条记录的完整原文。
- `×` 用于删除单条历史。
- 当前版本最多保留最近约 50 条历史记录。
- 退出或重启软件后，历史记录会清空。

## 5. 导出 BOM CSV

### 5.1 操作步骤

1. 在监听页收集至少一个元件编号。
2. 点击“BOM CSV 文件”旁的“浏览”，选择 `.csv` 保存位置。
3. 也可以手动输入完整路径，输入后点击“应用”。
4. 选择“中文”或“英文”元数据。
5. 点击底部“导出 BOM CSV”，等待结果提示。

建议使用明确的绝对路径，例如：

```text
D:\Projects\MyBoard\bom\lcsc_bom.csv
```

### 5.2 CSV 内容

CSV 使用带 BOM 的 UTF-8 编码，字段为：

```text
lcsc_id,quantity,mpn,manufacturer,description,package,datasheet_url
```

| 字段 | 含义 |
| --- | --- |
| `lcsc_id` | 立创商城编号 |
| `quantity` | 当前会话中该编号的捕获次数 |
| `mpn` | 制造商料号 |
| `manufacturer` | 制造商 |
| `description` | 元件描述 |
| `package` | 封装名称 |
| `datasheet_url` | 数据手册地址 |

注意：

- 导出会覆盖目标 CSV，不会追加到旧文件。
- `quantity` 不是严格的设计用量，导出后应结合原理图/BOM 再核对。
- 获取单个元件元数据失败时，仍会保留 ID 和数量，其余字段可能为空。
- “元数据语言”是全局设置，在 BOM、Kepler 和 Aurora 区域修改时会同步；它与界面语言互不影响。

## 6. 导出前的共同准备

Kepler 和 Aurora 已集成在当前安装包内，不需要单独安装命令行工具，也不需要配置系统 `PATH`。

导出前请确认：

1. “匹配结果”中至少有一个 ID，否则导出按钮不可用。
2. 网络可以访问立创商城和 LCEDA/EasyEDA 相关接口。
3. 使用“浏览”选择具有写入权限的绝对目录。
4. 修改路径、库名或并行数后点击对应的“应用”。
5. 关闭正在占用目标 `.SchLib`、`.PcbLib` 或 KiCad 库文件的 EDA 软件。

不建议依赖 Kepler 默认的 `~/lib` 相对路径。Aurora 的相对路径会固定解析到程序所在目录，但正式库仍建议通过“浏览”选择明确的绝对目录。

## 7. KiCad / Kepler 导出

### 7.1 操作步骤

1. 进入“导出”页，在 Kepler 配置中选择导出目录。
2. 选择“完整”“符号”“封装”或“3D”。
3. 按需设置“出错继续”“覆盖”“项目相对路径”和并行任务数。
4. 点击“导出 Kepler”。
5. 查看实时进度和最终的 `Success`、`Failed`、失败 ID 与输出目录。

### 7.2 导出模式

| 模式 | 输出内容 |
| --- | --- |
| 完整 | 符号、封装和 3D 模型，封装中写入 3D 模型引用 |
| 符号 | 仅生成或更新 `.kicad_sym` |
| 封装 | 仅生成 `.kicad_mod`，不附带 3D 模型引用 |
| 3D | 仅生成 `.wrl`，上游提供时同时生成 `.step` |

假设导出目录为 `D:\KiCad\MyLib`，典型输出为：

```text
D:\KiCad\MyLib\
├─ MyLib.kicad_sym
├─ MyLib.pretty\
│  └─ 元件名_Cxxxx.kicad_mod
├─ MyLib.3dshapes\
│  ├─ 模型名_Cxxxx.wrl
│  └─ 模型名_Cxxxx.step
└─ .checkpoint
```

库基础名由导出目录最后一级名称决定。需要改变库名时，请修改导出目录名称。

### 7.3 Kepler 选项

| 选项 | 作用 |
| --- | --- |
| 出错继续 | 单个 ID 失败后继续处理其余 ID |
| 覆盖 | 重新处理已记录完成的 ID，并覆盖同名符号、封装和模型；不会清空整个库 |
| 项目相对路径 | 在“完整”模式下将 3D 引用写为 `${KIPRJMOD}/MyLib.3dshapes/...` |
| 并行任务数 | 多 ID 同时处理的数量，至少为 1，默认 4 |

并行数过高可能触发上游接口限流。遇到间歇性网络失败时，建议降到 `1` 或 `2` 后重试。

> 当前版本限制
>
> 当前 `1.0.6` 内置 Kepler `1.0.25` 兼容实现，始终写入同一组 KiCad 库。“合并”“合并追加”和“库名称”目前只保存界面状态，不改变实际输出；关闭“合并”也不会按 ID 建立独立库。当前 Kepler 构建也不支持英文 LCSC 元数据，中文/英文选择不会改变 KiCad 输出内容。

### 7.4 在 KiCad 中使用

- 符号库：打开“管理符号库”，添加生成的 `.kicad_sym`。
- 封装库：打开“管理封装库”，添加生成的 `.pretty` 文件夹。
- 使用“项目相对路径”时，导出目录应与 KiCad 项目根目录关系正确，否则 `${KIPRJMOD}` 无法找到 `.3dshapes`。
- 修改 3D 路径设置后，需要开启“覆盖”重新生成封装。

## 8. Altium / Aurora 导出

### 8.1 操作步骤

1. 在 Aurora 配置中选择导出目录。
2. 选择“完整”“SchLib”或“PcbLib”。
3. 按需选择“合并”“合并追加”“出错继续”和“强制”。
4. 启用“合并”后填写库名称；留空时默认使用 `SeExMerged`。
5. 设置并行任务数，点击“导出 Altium 库”。

### 8.2 导出模式

| 模式 | 输出内容 |
| --- | --- |
| 完整 | 同时生成 `.SchLib` 和 `.PcbLib` |
| SchLib | 仅生成原理图库 |
| PcbLib | 仅生成封装库；上游有 STEP 时会嵌入 PcbLib |

未启用“合并”时，典型输出为：

```text
导出目录\
├─ schlib\
│  └─ 上游元件名_Cxxxx.SchLib
├─ pcblib\
│  └─ 上游封装名_Cxxxx.PcbLib
```

文件名和库内元件名始终附加精确匹配到的立创编号，避免不同料号共用同名封装时互相覆盖或串用 3D 模型。

启用“合并”并将库名设为 `MyLib` 时，输出为：

```text
导出目录\
├─ MyLib.SchLib
└─ MyLib.PcbLib
```

### 8.3 Aurora 选项

| 选项 | 作用 |
| --- | --- |
| 合并 | 使用当前全部匹配 ID 重建一对指定名称的库；不是只写本次新增 ID |
| 合并追加 | 读取现有同名合并库，跳过已存在 ID，再写回原文件 |
| 出错继续 | 单个 ID 失败后继续处理其他 ID |
| 强制 | 非合并模式下重新生成已存在的单元件库；关闭时会先校验现有库结构再决定是否复用 |
| 库名称 | 仅合并模式有效，填写不带 `.SchLib/.PcbLib` 扩展名的基础名 |
| 并行任务数 | 仅非合并批量导出实际生效，至少为 1 |

“合并追加”注意事项：

- 开启追加会自动开启“合并”，关闭“合并”也会关闭追加。
- “完整 + 合并追加”要求同名 `.SchLib` 和 `.PcbLib` 同时存在或同时不存在。
- 只剩一个库文件时会失败；先备份后补齐文件，或移走残缺文件并重新合并。
- 当前不支持“PcbLib + 合并追加”。需要追加时请选择“完整”或“SchLib”。
- 目录和库名必须与原合并库完全一致。

元数据语言：

- 中文：使用 LCEDA/EasyEDA 原始属性，通常以中文为主。
- 英文：额外请求 LCSC 商品详情，主要影响 SchLib 的描述、厂商、料号和数据手册，不影响 PcbLib。
- 英文接口不可用时，可切回中文后重试。

正确性保护：

- C 编号必须与 LCEDA 搜索结果精确一致；没有精确结果时不会回退导出其他器件。
- 封装声明了 3D 模型时，STEP 下载、格式或变换校验失败会让该 ID 导出失败，不会静默生成缺少模型的 PcbLib。
- 无法识别或会被丢弃的封装图元会明确失败；成功结果不会包含已知的有损降级。
- SchLib/PcbLib 先写入同目录临时文件，完成同步后再替换目标文件，写入失败时保留原库。

### 8.4 在 Altium Designer 中使用

- 可以直接打开生成的 `.SchLib` 和 `.PcbLib` 检查内容。
- 也可以在 Altium 的文件型库管理界面中添加生成的库；不同版本菜单名称可能略有差异。
- 使用前应检查符号引脚、封装焊盘、层映射和 3D 模型。

## 9. 语言、主题与侧栏

- 首次启动时，界面语言跟随系统语言；之后会记住手动选择。
- “语言”页可以切换中文和英文。
- 左下角主题按钮切换白天/黑夜模式，并记住选择。
- 侧栏顶部按钮用于收起或展开导航栏。
- 元数据语言只影响 BOM/导出数据，不会切换界面语言。

## 10. 配置、数据与隐私

- `export_config.json`：保存在程序同目录，记录导出目录、模式、库名、并行数和元数据语言等设置。
- 历史与匹配结果：只保存在内存中，退出程序后清空。
- `.checkpoint`：仅由当前内置 Kepler/KiCad 导出使用；Aurora/Altium 导出以实际库文件校验结果为准。
- “清空全部”：只清除当前会话数据，不会删除已经导出的文件。
- 软件联网时会向立创商城、LCEDA/EasyEDA 等接口请求元件资料。

便携版备份时，可同时备份 `大壮的AD工具.exe` 和同目录下的 `export_config.json`。

## 11. 更新说明

“关于”页提供版本与更新入口，但 `1.0.6` 的自动更新可能受网络或发布配置影响。建议以可信来源发布的新版安装包/便携包为准，更新前备份 `export_config.json`。

安装新版时，先关闭正在运行的旧版本。便携版可把新 EXE 解压到新目录，再迁移配置文件，避免直接覆盖后无法回退。

## 12. 常见问题

### 12.1 复制后没有匹配结果

1. 确认状态是“监听中”。
2. 确认文本包含 `C` 加至少 3 位数字，例如 `C2040`。
3. 软件启动后重新复制一次，不要只依赖启动前的旧剪贴板内容。
4. 连续完全相同的文本可能被忽略，可先复制其他文本再重新复制元件信息。
5. 仍无结果时，暂停后恢复监听或重启软件。

### 12.2 导出按钮是灰色

- 当前没有匹配结果；先在监听页收集至少一个 ID。
- 或者同一导出任务正在运行，请等待任务结束。

### 12.3 BOM 中描述、封装或数据手册为空

- 检查网络和立创商城接口是否可访问。
- 切换中文/英文元数据后重试。
- 个别元件可能已下架或上游资料不完整，ID 和数量仍会保留。

### 12.4 Kepler 显示 `Nothing to do` 或大量 `Skipped`

导出目录下的 `.checkpoint` 已记录这些 ID。需要重新生成时优先开启“覆盖”；也可以在确认不需要恢复记录并完成备份后删除 `.checkpoint`。

### 12.5 KiCad 中看不到 3D 模型

- 确认使用“完整”或“3D”模式，且上游元件确实提供 3D 模型。
- 检查 `.3dshapes` 是否与库一起移动。
- 使用 `${KIPRJMOD}` 时，确认 KiCad 项目根目录和导出目录关系正确。
- 修正设置后开启“覆盖”重新生成封装。

### 12.6 Aurora 显示 `Skipped`

合并追加模式检测到库中已有同一 ID 时会跳过，这是正常行为。非合并模式不再依赖 `.checkpoint`；需要无条件重做现有单元件库时开启“强制”。

### 12.7 合并追加失败

- 确认已开启“合并”。
- 确认目录和库名称与原库一致。
- “完整”模式下确认 `.SchLib` 与 `.PcbLib` 成对存在。
- 不要在“PcbLib”单项模式下使用合并追加。
- 关闭 Altium，避免目标库文件被占用。

### 12.8 写入失败或 I/O 错误

- 使用本地、可写的绝对目录。
- 检查磁盘空间和目录权限。
- 关闭正在打开目标库的 KiCad/Altium。
- 便携版不要放在 ZIP、只读目录或受保护的系统目录内运行。

### 12.9 软件无法启动或界面空白

- 确认已经完整解压便携包。
- Windows 10/11 通常自带 WebView2；缺失时安装 Microsoft Edge WebView2 Runtime。
- 若安全软件拦截，先核对文件来源和 SHA-256，不要直接忽略风险提示。

## 13. 发布文件校验

以下哈希对应 2026-07-11 生成的 `1.0.6` 本地发布包：

| 文件 | SHA-256 |
| --- | --- |
| `大壮的AD工具_1.0.6_x64-setup.exe` | `E079DE3AD5742DF2119ACB87780084E4BF1C0A0F898765A7DE46C226F92E2E3B` |
| `大壮的AD工具_1.0.6_x64_portable.zip` | `4D4BF371BD13233AF822D55FAD9AB376F750311E6DE794DDD3FBA0321E5545A4` |
| `nova.exe` | `A7D5A67D5AA2B7FB730F0C7CC07C52476768CA72E2D6F21AF5F1AE85BC373D4E` |

只要文件被重新打包或修改，SHA-256 就会变化。

## 14. 使用建议

- 先用少量元件验证输出目录和 EDA 导入流程，再进行大批量导出。
- 重要库在合并、追加或覆盖前先备份。
- 自动生成的符号、封装和 3D 模型必须人工复核后再用于打样或生产。
- BOM 数量、极性、引脚编号、封装尺寸、焊盘、层映射和 3D 高度都应以原厂资料及实际设计为准。
